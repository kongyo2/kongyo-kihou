# 変更履歴

## 0.2.0

### 追加

- **「kongyo 判定待ち」ビュー**（エクスプローラ）。期日到来と期日前を分けて並べ、バッジに期日到来の件数を出す。行を選べば台帳の該当行へ跳び、期日到来の行では `○` `×` `－` をその場で押せる。台帳の無いワークスペースには現れない
- **起動時の期日到来通知**（`kongyo.notifications.due`）。判定日が来た予測があれば一度だけ知らせる
- **アウトライン**（DocumentSymbol）。各行が `記号 型 対象` の形で並び、パンくずからも跳べる
- **集計ビューに「判定待ち（期日到来）」の一覧**。§9「週一で末尾記号を置換する」の対象がそのまま見える
- **`kongyo.judge` の連続判定**。期日到来を捌き切るまで一覧に戻る
- **ウォークスルー**（はじめに）。鋳造 → 直接書く → 判定 → 集計 → 在庫の五歩
- **拡張アイコン**。`→` が `○ × －` へ置換される図案。`npm run icon` で決定的に再生成できる
- ホバーの拡充：末尾記号（`→` `○` `×` `－`）と刻印の意味を出す
- 補完の拡充：`C`（条件）に状態リテラル候補、鋳造ウィザードの `D` に日付の形式例

### 修正

- **型と項のあいだに書いた語が、確定・整形で黙って消えていた。** どの項にも属さない語は G-STRAY として却下し、整形（`canonicalize`）はその行に触れない
- **必須項の Quick Fix が末尾記号の後ろへ挿入されていた。** 記号の手前に挿すようにした
- **Markdown の ```` ```kgy ````・大文字のフェンスが、色は付くのに検査されなかった。** 注入文法と同じものを読むようにし、閉じフェンスは開きと同じ文字・同じ長さ以上（CommonMark と同じ）だけを認める
- **台帳ガードの変更イベント処理を直列化。** 速い連続編集で「前回見た本文」の控えを取り合い、判定がずれ得た
- `guardController.ts` に生の NUL バイトが埋め込まれ、git がファイルをバイナリ扱いしていた。エスケープ表記 `\x00` へ置き換えた（実行時の挙動は同じ）
- CodeLens とホバーの残り日数を暦日で数え、「あと 1 日」が今日を指す読み違いを消した（「今日が期日」と出す）

### 整理

- フェンス検出を `src/core/fence.ts` へ移し、単体テストの対象にした
- 鋳造ウィザードを `src/extension/wizard.ts` へ、コマンド共有部を `src/extension/context.ts` へ分離
- `dueEntries` を `pendingEntries` の絞り込みとして一本化。`D` を読めない行の番兵 `NO_DEADLINE` を公開
- 検査の組み立てから `Builder` 間接層を除去。`pad2` の重複を畳んだ
- 未使用の輸出（`parseLines` / `isBlank` / `escapeRegex`）を削除
- TextMate 文法の禁止語彙・選言・例外の一覧を §5（`vocabulary.ts`）と同期
- 規則ビューの必須項表を七項に揃え、追加の項（`T` `E` `S₁` `S₂` `比較`）を別掲にした

## 0.1.0

最初の版。

- `kongyo` 言語（`.kongyo` / `.kgy`）と、Markdown 内の ```` ```kongyo ```` フェンスの認識
- 打鍵ごとの静的検査：必須項（七項）、C1 前後性、C2 外部判定、C3 反証形、C4 単一観測、C5 数値確率、C6 例外閉包、C7 停止条件、C8 難度標示、選言の禁止、§5 禁止語彙
- 反証文の自動生成。生成できない行は C3 に落ちる
- 鋳造ウィザード（`Ctrl+Alt+K`）。一項ずつ訊き、打鍵ごとに検査する
- 確定（`Ctrl+Alt+Enter`）。却下が残っていれば確定できない
- 台帳の拘束：保存済みの確定行は本文を書き換えられず、編集は差し戻される。末尾記号の置換だけが許される
- 拘束の一時解除。解除・C6 事後追加・判定書換は台帳に記録として残る
- 判定（CodeLens の `○` `×` `－`、およびコマンド）
- 集計：`×` の絶対数、`－` の絶対数、`[易]` を除いたブライアスコア。`○` の数は集計しない
- 未形式化在庫（`Ctrl+Alt+I`）と、その増加率
- 補完、ホバー、インレイヒント、Quick Fix、整形、装飾、ステータスバー
