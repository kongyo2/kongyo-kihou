# 変更履歴

## 0.1.0

最初の版。

- `kongyo` 言語（`.kongyo` / `.kgy`）と、Markdown 内の ```` ```kongyo ```` フェンスの認識
- 打鍵ごとの静的検査：必須項（七項）、C1 前後性、C2 外部判定、C3 反証形、C4 単一観測、C5 数値確率、C6 例外閉包、C7 停止条件、C8 難度標示、選言の禁止、§5 禁止語彙
- 反証文の自動生成。生成できない行は C3 に落ちる
- 鋳造ウィザード（`Ctrl+Alt+K`）。一項ずつ訊き、打鍵ごとに検査する
- 確定（`Ctrl+Alt+Enter`）。却下が残っていれば確定できない
- 台帳の拘束：保存済みの確定行は本文を書き換えられず、編集は差し戻される。末尾記号の置換だけが許される
- 拘束の一時解除。解除・C6 事後追加・判定書換は台帳に記録として残る
- 判定（CodeLens の `○` `×` `－`、およびコマンド）
- 集計：`×` の絶対数、`－` の絶対数、`[易]` を除いたブライアスコア。`○` の数は集計しない
- 未形式化在庫（`Ctrl+Alt+I`）と、その増加率
- 補完、ホバー、インレイヒント、Quick Fix、整形、装飾、ステータスバー
